name: deploy
# Auto-deploy to Vercel ONLY when an implement PR is merged to master.
# Research/plan PR merges do NOT trigger deployment.

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  # Check if this push came from merging an implement PR
  check-implement:
    runs-on: ubuntu-latest
    outputs:
      should-deploy: ${{ steps.check.outputs.should_deploy }}
      pr-num: ${{ steps.check.outputs.pr_num }}
    permissions:
      pull-requests: read
    steps:
      - name: Check PR labels for workflow/implement
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          COMMIT_SHA="${{ github.sha }}"
          echo "Checking if commit $COMMIT_SHA came from an implement PR..."

          # Find the merged PR that contains this commit
          PR_NUM=$(gh api "repos/${{ github.repository }}/commits/${COMMIT_SHA}/pulls" \
            --jq '.[] | select(.state == "closed" and .merged_at != null) | .number' \
            2>/dev/null | head -1)

          if [ -z "$PR_NUM" ]; then
            echo "No merged PR found for commit. Skipping deploy."
            echo "should_deploy=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo "Found merged PR #$PR_NUM"

          # Check if it has workflow/implement label
          HAS_LABEL=$(gh api "repos/${{ github.repository }}/pulls/${PR_NUM}" \
            --jq '[.labels[].name] | any(. == "workflow/implement")' 2>/dev/null)

          if [ "$HAS_LABEL" = "true" ]; then
            echo "PR #$PR_NUM has workflow/implement label → deploy!"
            echo "should_deploy=true" >> $GITHUB_OUTPUT
            echo "pr_num=$PR_NUM" >> $GITHUB_OUTPUT
          else
            echo "PR #$PR_NUM does NOT have workflow/implement label → skip deploy."
            echo "should_deploy=false" >> $GITHUB_OUTPUT
          fi

  deploy:
    needs: check-implement
    if: needs.check-implement.outputs.should-deploy == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6

      - name: Deploy to Vercel
        id: deploy
        uses: amondnet/vercel-action@v42
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'

      - name: Notify Feishu
        if: always()
        env:
          STATUS: ${{ job.status }}
          PR_NUM: ${{ needs.check-implement.outputs.pr-num }}
          RUN_NUM: ${{ github.run_number }}
          DEPLOY_URL: ${{ steps.deploy.outputs.url }}
        run: |
          EMOJI="[ok]"
          [ "$STATUS" != "success" ] && EMOJI="[fail]"
          MSG="${EMOJI} [PDA] deploy #${RUN_NUM} ${STATUS} (PR #${PR_NUM})"
          [ -n "$DEPLOY_URL" ] && MSG="${MSG}\n${DEPLOY_URL}"
          curl -s -X POST -H "Content-Type: application/json" \
            -d "{\"msg_type\":\"text\",\"content\":{\"text\":\"${MSG}\"}}" \
            https://open.feishu.cn/open-apis/bot/v2/hook/390781d4-285c-4d66-afa4-1aadfb76a7b9

  # Skip notification for non-implement merges
  skip-notify:
    needs: check-implement
    if: needs.check-implement.outputs.should-deploy != 'true'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Non-implement merge — deployment skipped."
